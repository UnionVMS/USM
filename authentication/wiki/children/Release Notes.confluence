h2. USM Authentication Release Notes

These release notes refer to version *${version}* of the USM Authentication Module

h2. Introduction

The USM Authentication Module is composed of the authentication service and the authentication REST service.
 

h2. Summary

This release includes modifications implemented during *Sprint 7* of the USM project.

h2. Implemented User Stories contained by current release

